# Production Application Configuration (Stable)
application:
  name: DevOpsSimulator
  version: 1.0.0
  environment: production
  
server:
  host: 0.0.0.0
  port: 8080
  ssl_enabled: true
  
logging:
  level: INFO
  format: json
  output: /var/log/app.log
  
database:
  host: prod-db.example.com
  port: 5432
  name: devops_prod
  
features:
  auto_scaling: true
  load_balancer: enabled
  backup_enabled: true

# Development Application Configuration (Stable)
development:
  mock_external_apis: true
  debug_mode: true
  cors_enabled: true
  
notifications:
  slack_webhook: "https://hooks.slack.com/services/XXX"
  discord_webhook: "https://discord.com/api/webhooks/YYY"
  email_alerts: false

---
# 🧪 EXPERIMENTAL CONFIGURATION (UNSTABLE)
# This block is merged from 'conflict-simulator' but kept separate and disabled.
experimental:
  enabled: false # CRITICAL: Keep this set to false!
  version: 3.0.0-experimental
  environment: testing
  
  server:
    port: 9000
    multi_port: [9000, 9001, 9002]
    
  logging:
    level: TRACE
    format: structured
    ai_analysis: enabled
    
  database:
    host: cluster.example.com
    distributed: true
    
  ai_features:
    predictive_scaling: true
    anomaly_detection: true
    model_version: "v2.1.0"
    
  security:
    zero_trust: enabled
    encryption: AES-256"// Added new feature flag" 
